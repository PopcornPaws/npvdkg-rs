use crate::types::{Contribution, Participant};

pub fn calculate_contribution(
    participants: Vec<Participant>,
    me: Participant,
    old_sh: Option<SecretKey>,
    g2: G2,
    ) -> Contribution {
    todo!();
}

pub fn calculate_my_key() -> CalculatedShare {
    todo!();
}
